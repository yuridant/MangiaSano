# Database
POSTGRES_DB=mangiasano
POSTGRES_USER=mangiasano
POSTGRES_PASSWORD=mangiasano

# Backend
DATABASE_URL=postgresql://mangiasano:mangiasano@postgres:5432/mangiasano?schema=public
JWT_SECRET=change-me-in-production
PORT=3000
APP_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173

# OpenAI
OPENAI_API_KEY=sk-...

# SMTP (opzionale per inviti via email)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=demo
SMTP_PASS=demo
SMTP_FROM=no-reply@mangiasano.local

# Frontend
VITE_API_URL=http://localhost:3000
